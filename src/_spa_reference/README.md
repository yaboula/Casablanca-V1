# SPA Reference Only

This directory contains the quarantined Emergent CRA/React Router SPA material.

Rules:

- Do not import from this directory into `src/app`.
- Do not use `AppStore`, fake reservations, fake refs, or mock business state in production routes.
- Use this only as visual and UX reference while rebuilding features on the Next App Router foundation.
- The production frontend runtime is `src/app`.
