# Gemini Adapter

Read `AGENTS.md` first.

The canonical multi-agent operating system is in `.agents/`.

For non-trivial tasks:
- use `.agents/activation-guide.md`
- create a Task Packet
- route through PM Orchestrator
- follow workflows
- respect blocking rules
- require QA before closure
- produce a Handoff Summary

Do not duplicate `.agents/` content here.
